- name: install nginx
  hosts: local
  connection: local
  become: yes
  tasks:

  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start and enable service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes